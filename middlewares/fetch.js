/**
 *
 * @returns {Function}
 */
const factory = () => {

	//todo, actually fetch the data to the model
	// call between pre and post cache
	return async (req, res, next) => {

	};
};

/**
 * User: Oleg Kamlowski <n@sovrin.de>
 * Date: 20.12.2018
 * Time: 00:07
 */
module.exports = factory;
